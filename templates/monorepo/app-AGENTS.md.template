<!-- Nested context, ≤30 lines. AGENTS.md nests at ANY depth, not only
     apps/*: this directory earns its own file only because it holds
     non-inferable local knowledge — its own commands, its own gotchas —
     never for symmetry with a sibling. Only THIS directory's facts; the
     root AGENTS.md covers everything shared and stays authoritative
     except where this file is deliberately more specific (nearest-wins).
     Ships with its own ≤3-line CLAUDE.md pointer beside it. -->
# {{APP_NAME}}

{{APP_TRACKER_PROJECT}}
<!-- OPTIONAL: this domain's `Tracker-project:` line, format and inheritance
     per reference/tracker.md. Delete the marker when the domain has no
     project of its own — it then inherits the root declaration whole. -->

{{APP_SUMMARY}}

## Commands

{{APP_COMMANDS}}

## Gotchas

{{APP_GOTCHAS}}
