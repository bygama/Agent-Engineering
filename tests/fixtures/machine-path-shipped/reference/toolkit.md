# toolkit reference

Configs are read from `/home/anna/configs/` when the toolkit runs on the
author's workstation — a POSIX user-home path, not a portable one.

Vendored helpers live under `/opt/toolchain/bin` and need no local changes;
that is an illustrative Unix path, not anyone's home directory.
