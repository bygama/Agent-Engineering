<!-- lint fixture: a lane that merely QUOTES the marker inside a fenced
     command transcript (MAT-115) — it is not declaring the design-first
     approval window, so PLAN.md must still be required. -->
# lane-window-quoted-fixture

Standard: AE/1.4.2

Small demo repo whose only lane quotes the marker without being in the window.

## Commands

- `make check` # not verified
