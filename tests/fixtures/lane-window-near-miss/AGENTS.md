<!-- lint fixture: a lane whose PROGRESS.md paraphrases the design-first
     marker instead of carrying it verbatim (MAT-115) — must NOT be exempted
     from lane-incomplete. Otherwise lint-clean. -->
# lane-window-near-miss-fixture

Standard: AE/1.4.2

Small demo repo whose only lane's marker text is a near-miss paraphrase.

## Commands

- `make check` # not verified
