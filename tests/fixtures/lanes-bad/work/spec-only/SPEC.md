# spec-only lane — spec

A lane that holds only SPEC.md — no PROGRESS.md, no PLAN.md. Pins that a
lane outside the design-first approval window (no marker-carrying
PROGRESS.md at all) still fires both `lane-incomplete` findings (MAT-115).
